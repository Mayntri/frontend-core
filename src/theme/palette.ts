import { PaletteOptions } from "@mui/material";

export const palette = (theme: any): PaletteOptions => {
    return {
        mode: 'light',
        primary: {
            main: '#cd07e6'
        }
    };
}